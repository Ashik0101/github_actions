on:
  push:
jobs:
  first-job:
    runs-on: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - name: Check node version
        run: node --version
      - name: Check npm version
        run: npm --version
